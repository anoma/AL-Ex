# AL repository instructions

- AL surface syntax does not support Elixir tuple literals. Use lists, maps, or value objects for AL data.
- VM-internal changes do not require image, replay, or stored-state compatibility unless the user explicitly requests it.
